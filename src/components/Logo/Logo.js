import React from 'react';

import classes from './Logo.css';

const logo = props => {
    return (
        <img 
            alt="Logo"
            className={classes.Logo}></img>
    )
};

export default logo;